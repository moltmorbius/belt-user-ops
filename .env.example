# Belt Indexer GraphQL endpoint (optional, defaults to production)
BELT_INDEXER_URL=https://belt-indexer-production.up.railway.app/graphql

# Discord webhook URL for posting notifications (required)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Redis URL for state tracking and deduplication (optional, recommended for production)
# If not set, monitor will work but without deduplication
REDIS_URL=redis://default:password@host:port
